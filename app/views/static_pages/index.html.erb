<%= form_with url: "", method: :get do |form| %>
  <%= form.label :user_id %>
  <%= form.text_field :user_id %>
  <%= form.submit "Show images" %>
<% end %>

<% if @photos %>
  <% @photos_urls.each do |photo_url| %>
    <img src="<%= photo_url %>" alt="user-photo">
  <% end %>
<% end %>
